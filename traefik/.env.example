# GitHub secret ENV_traefik. Double every $ in the htpasswd hash so Compose keeps them.
#   htpasswd -nb admin 'password' | sed 's/\$/\$\$/g'
DOMAIN=example.com
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$hash$$here
