# DOMAIN comes from the repo variable; the rest goes in the ENV_keycloak secret.
DOMAIN=example.com

# First-boot admin account (remove from the secret after creating a real admin)
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=changeme

# Shared by keycloak and the postgres sidecar
KC_DB_PASSWORD=changeme
