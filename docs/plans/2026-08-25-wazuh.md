# Wazuh Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Deploy Wazuh 4.14.7 (indexer + manager + dashboard) as a `wazuh/` infra stack behind Traefik, with a native Ubuntu agent on this VPS and Keycloak SAML SSO at `https://wazuh.<domain>`.

**Architecture:** Official wazuh-docker single-node, adapted to this repo’s rules: only Traefik publishes 80/443; manager binds `127.0.0.1:1514/1515` for the host agent; data and certs live under `/opt/data/wazuh`; dashboard is the only service on `proxy`. See `docs/plans/2026-08-25-wazuh-design.md`.

**Tech Stack:** Docker Compose, Traefik v3.6, Wazuh 4.14.7 images, native `wazuh-agent` deb, Keycloak SAML (`XhampsHub` realm), GitHub Actions deploy matrix.

**Design:** `docs/plans/2026-08-25-wazuh-design.md`

---

### Task 1: Vendor official 4.14.7 single-node configs

**Files:**
- Create: `wazuh/config/certs.yml`
- Create: `wazuh/config/wazuh_cluster/wazuh_manager.conf`
- Create: `wazuh/config/wazuh_indexer/wazuh.indexer.yml`
- Create: `wazuh/config/wazuh_indexer/internal_users.yml`
- Create: `wazuh/config/wazuh_dashboard/opensearch_dashboards.yml`
- Create: `wazuh/config/wazuh_dashboard/wazuh.yml`
- Create: `wazuh/generate-indexer-certs.yml`

**Step 1: Download upstream files**

From the repo root:

```bash
mkdir -p wazuh/config/wazuh_cluster wazuh/config/wazuh_indexer wazuh/config/wazuh_dashboard
BASE=https://raw.githubusercontent.com/wazuh/wazuh-docker/v4.14.7/single-node
curl -fsSL "$BASE/config/certs.yml" -o wazuh/config/certs.yml
curl -fsSL "$BASE/config/wazuh_cluster/wazuh_manager.conf" -o wazuh/config/wazuh_cluster/wazuh_manager.conf
curl -fsSL "$BASE/config/wazuh_indexer/wazuh.indexer.yml" -o wazuh/config/wazuh_indexer/wazuh.indexer.yml
curl -fsSL "$BASE/config/wazuh_indexer/internal_users.yml" -o wazuh/config/wazuh_indexer/internal_users.yml
curl -fsSL "$BASE/config/wazuh_dashboard/opensearch_dashboards.yml" -o wazuh/config/wazuh_dashboard/opensearch_dashboards.yml
curl -fsSL "$BASE/config/wazuh_dashboard/wazuh.yml" -o wazuh/config/wazuh_dashboard/wazuh.yml
curl -fsSL "$BASE/generate-indexer-certs.yml" -o wazuh/generate-indexer-certs.yml
```

**Step 2: Point the cert generator at `/opt/data`**

Replace the volumes in `wazuh/generate-indexer-certs.yml` so generated certs never live in the rsynced tree:

```yaml
# Wazuh App Copyright (C) 2017, Wazuh Inc. (License GPLv2)
services:
  generator:
    image: wazuh/wazuh-certs-generator:0.0.4
    hostname: wazuh-certs-generator
    environment:
      - CERT_TOOL_VERSION=4.14
    volumes:
      - /opt/data/wazuh/certs:/certificates/
      - ./config/certs.yml:/config/certs.yml:ro
```

**Step 3: Enable enrollment password on the manager**

In `wazuh/config/wazuh_cluster/wazuh_manager.conf`, change the `<auth>` block:

```xml
  <auth>
    <disabled>no</disabled>
    <port>1515</port>
    <use_source_ip>no</use_source_ip>
    <purge>yes</purge>
    <use_password>yes</use_password>
```

**Step 4: Verify files exist and are non-empty**

```bash
wc -l wazuh/config/certs.yml \
  wazuh/config/wazuh_cluster/wazuh_manager.conf \
  wazuh/config/wazuh_indexer/wazuh.indexer.yml \
  wazuh/config/wazuh_indexer/internal_users.yml \
  wazuh/config/wazuh_dashboard/opensearch_dashboards.yml \
  wazuh/config/wazuh_dashboard/wazuh.yml \
  wazuh/generate-indexer-certs.yml
grep -n 'use_password>yes' wazuh/config/wazuh_cluster/wazuh_manager.conf
grep -n '/opt/data/wazuh/certs' wazuh/generate-indexer-certs.yml
```

Expected: all files have lines > 0; `use_password>yes` matches; certs path is `/opt/data/wazuh/certs`.

**Step 5: Commit**

```bash
git add wazuh/
git commit -m "Vendor Wazuh 4.14.7 single-node configs."
```

---

### Task 2: Compose stack (no public ports except localhost agent)

**Files:**
- Create: `wazuh/docker-compose.yml`
- Create: `wazuh/.env.example`

**Step 1: Write compose**

```yaml
# Wazuh 4.14.7 single-node. Dashboard at wazuh.<domain> via Traefik.
# Manager 1514/1515 bound to 127.0.0.1 only — host agent, not the internet.
# Requires /opt/infra/wazuh/.env from ENV_wazuh + DOMAIN.
services:
  wazuh.indexer:
    image: wazuh/wazuh-indexer:4.14.7
    hostname: wazuh.indexer
    restart: unless-stopped
    environment:
      OPENSEARCH_JAVA_OPTS: -Xms1g -Xmx1g
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - /opt/data/wazuh/indexer:/var/lib/wazuh-indexer
      - /opt/data/wazuh/certs/root-ca.pem:/usr/share/wazuh-indexer/config/certs/root-ca.pem:ro
      - /opt/data/wazuh/certs/wazuh.indexer-key.pem:/usr/share/wazuh-indexer/config/certs/wazuh.indexer.key:ro
      - /opt/data/wazuh/certs/wazuh.indexer.pem:/usr/share/wazuh-indexer/config/certs/wazuh.indexer.pem:ro
      - /opt/data/wazuh/certs/admin.pem:/usr/share/wazuh-indexer/config/certs/admin.pem:ro
      - /opt/data/wazuh/certs/admin-key.pem:/usr/share/wazuh-indexer/config/certs/admin-key.pem:ro
      - ./config/wazuh_indexer/wazuh.indexer.yml:/usr/share/wazuh-indexer/config/opensearch.yml:ro
      - ./config/wazuh_indexer/internal_users.yml:/usr/share/wazuh-indexer/config/opensearch-security/internal_users.yml:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sk https://localhost:9200 >/dev/null"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 180s
    networks: [internal]

  wazuh.manager:
    image: wazuh/wazuh-manager:4.14.7
    hostname: wazuh.manager
    restart: unless-stopped
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 655360
        hard: 655360
    ports:
      - "127.0.0.1:1514:1514"
      - "127.0.0.1:1515:1515"
    environment:
      INDEXER_URL: https://wazuh.indexer:9200
      INDEXER_USERNAME: admin
      INDEXER_PASSWORD: ${INDEXER_PASSWORD}
      FILEBEAT_SSL_VERIFICATION_MODE: full
      SSL_CERTIFICATE_AUTHORITIES: /etc/ssl/root-ca.pem
      SSL_CERTIFICATE: /etc/ssl/filebeat.pem
      SSL_KEY: /etc/ssl/filebeat.key
      API_USERNAME: wazuh-wui
      API_PASSWORD: ${API_PASSWORD}
    volumes:
      - /opt/data/wazuh/manager/api:/var/ossec/api/configuration
      - /opt/data/wazuh/manager/etc:/var/ossec/etc
      - /opt/data/wazuh/manager/logs:/var/ossec/logs
      - /opt/data/wazuh/manager/queue:/var/ossec/queue
      - /opt/data/wazuh/manager/var:/var/ossec/var/multigroups
      - /opt/data/wazuh/manager/integrations:/var/ossec/integrations
      - /opt/data/wazuh/manager/active-response:/var/ossec/active-response/bin
      - /opt/data/wazuh/manager/agentless:/var/ossec/agentless
      - /opt/data/wazuh/manager/wodles:/var/ossec/wodles
      - /opt/data/wazuh/manager/filebeat-etc:/etc/filebeat
      - /opt/data/wazuh/manager/filebeat-var:/var/lib/filebeat
      - /opt/data/wazuh/certs/root-ca.pem:/etc/ssl/root-ca.pem:ro
      - /opt/data/wazuh/certs/wazuh.manager.pem:/etc/ssl/filebeat.pem:ro
      - /opt/data/wazuh/certs/wazuh.manager-key.pem:/etc/ssl/filebeat.key:ro
      - ./config/wazuh_cluster/wazuh_manager.conf:/wazuh-config-mount/etc/ossec.conf:ro
      - /opt/data/wazuh/authd.pass:/var/ossec/etc/authd.pass:ro
    depends_on:
      wazuh.indexer:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/var/ossec/bin/wazuh-control", "status"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks: [internal]

  wazuh.dashboard:
    image: wazuh/wazuh-dashboard:4.14.7
    hostname: wazuh.dashboard
    restart: unless-stopped
    environment:
      INDEXER_USERNAME: admin
      INDEXER_PASSWORD: ${INDEXER_PASSWORD}
      WAZUH_API_URL: https://wazuh.manager
      DASHBOARD_USERNAME: kibanaserver
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD}
      API_USERNAME: wazuh-wui
      API_PASSWORD: ${API_PASSWORD}
    volumes:
      - /opt/data/wazuh/certs/wazuh.dashboard.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem:ro
      - /opt/data/wazuh/certs/wazuh.dashboard-key.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem:ro
      - /opt/data/wazuh/certs/root-ca.pem:/usr/share/wazuh-dashboard/certs/root-ca.pem:ro
      - ./config/wazuh_dashboard/opensearch_dashboards.yml:/usr/share/wazuh-dashboard/config/opensearch_dashboards.yml:ro
      - ./config/wazuh_dashboard/wazuh.yml:/usr/share/wazuh-dashboard/data/wazuh/config/wazuh.yml:ro
      - /opt/data/wazuh/dashboard-config:/usr/share/wazuh-dashboard/data/wazuh/config
      - /opt/data/wazuh/dashboard-custom:/usr/share/wazuh-dashboard/plugins/wazuh/public/assets/custom
    depends_on:
      wazuh.indexer:
        condition: service_healthy
      wazuh.manager:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sk https://localhost:5601 >/dev/null"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s
    networks: [proxy, internal]
    labels:
      traefik.enable: "true"
      traefik.http.routers.wazuh.rule: Host(`wazuh.${DOMAIN}`)
      traefik.http.routers.wazuh.tls.certresolver: le
      traefik.http.services.wazuh.loadbalancer.server.port: "5601"
      traefik.http.services.wazuh.loadbalancer.server.scheme: https
      traefik.http.services.wazuh.loadbalancer.serversTransport: wazuh-dashboard@file

networks:
  proxy:
    external: true
  internal: {}
```

**Step 2: Write `.env.example`**

v1 uses the official Wazuh demo hashes already in `internal_users.yml`. `ENV_wazuh` plaintext **must** match those hashes (or you must regenerate hashes). Rotate after first login via the Wazuh passwords tool if you want; then update `ENV_wazuh` and `internal_users.yml` together.

```bash
# DOMAIN comes from the repo variable; the rest goes in the ENV_wazuh secret.
DOMAIN=example.com

# Must match bcrypt hashes in config/wazuh_indexer/internal_users.yml
INDEXER_PASSWORD=SecretPassword
DASHBOARD_PASSWORD=kibanaserver

# Wazuh API (wazuh-wui) — must match wazuh.yml
API_PASSWORD=MyS3cr37P450r.*-

# Host agent enrollment (authd.pass). Randomize this.
ENROLL_PASSWORD=changeme
```

**Step 3: Verify compose has no 0.0.0.0 publishes**

```bash
grep -n 'ports:' -A3 wazuh/docker-compose.yml
grep -n '0.0.0.0' wazuh/docker-compose.yml || true
grep -n '127.0.0.1:1514' wazuh/docker-compose.yml
```

Expected: only `127.0.0.1:1514` and `127.0.0.1:1515`. No `9200`, `55000`, `514`, or `443` publishes. `0.0.0.0` must not appear.

**Step 4: Commit**

```bash
git add wazuh/docker-compose.yml wazuh/.env.example
git commit -m "Add Wazuh Compose stack with localhost-only agent ports."
```

---

### Task 3: Traefik skip-verify for the dashboard backend

**Files:**
- Create: `traefik/dynamic.yml`
- Modify: `traefik/traefik.yml`
- Modify: `traefik/docker-compose.yml`

**Step 1: Add file provider to static config**

In `traefik/traefik.yml`, under `providers:`:

```yaml
providers:
  docker:
    exposedByDefault: false
    network: proxy
  file:
    filename: /etc/traefik/dynamic.yml
```

**Step 2: Create `traefik/dynamic.yml`**

```yaml
# Scoped skip-verify for Wazuh dashboard's internal self-signed cert.
# Do not set insecureSkipVerify globally.
http:
  serversTransports:
    wazuh-dashboard:
      insecureSkipVerify: true
```

**Step 3: Mount it in Traefik compose**

Add volume next to `traefik.yml`:

```yaml
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic.yml:/etc/traefik/dynamic.yml:ro
      - /opt/data/traefik:/acme
```

**Step 4: Verify YAML keys**

```bash
grep -n 'filename: /etc/traefik/dynamic.yml' traefik/traefik.yml
grep -n 'insecureSkipVerify: true' traefik/dynamic.yml
grep -n 'dynamic.yml' traefik/docker-compose.yml
```

Expected: all three match.

**Step 5: Commit**

```bash
git add traefik/traefik.yml traefik/dynamic.yml traefik/docker-compose.yml
git commit -m "Let Traefik skip verify only for the Wazuh dashboard backend."
```

---

### Task 4: First-up script (certs, dirs, authd.pass)

**Files:**
- Create: `wazuh/first-up.sh`

This runs on the VPS (via deploy) as `deploy` with sudo. Idempotent.

**Step 1: Write the script**

```bash
#!/usr/bin/env bash
# Idempotent Wazuh host prep: sysctl, data dirs, certs, authd.pass.
# Run from /opt/infra/wazuh as deploy (sudo). Sourced .env must exist.
set -euo pipefail
cd "$(dirname "$0")"
# shellcheck disable=SC1091
set -a && source .env && set +a

sudo sysctl -w vm.max_map_count=262144
echo 'vm.max_map_count=262144' | sudo tee /etc/sysctl.d/99-wazuh.conf >/dev/null

sudo install -d -o deploy -g deploy /opt/data/wazuh /opt/data/wazuh/certs \
  /opt/data/wazuh/manager /opt/data/wazuh/dashboard-config /opt/data/wazuh/dashboard-custom
sudo install -d -o 1000 -g 1000 /opt/data/wazuh/indexer

if [ ! -f /opt/data/wazuh/certs/root-ca.pem ]; then
  docker compose -f generate-indexer-certs.yml run --rm generator
fi

# authd.pass is one line, mode 640, read by the manager container
umask 077
printf '%s\n' "${ENROLL_PASSWORD:?ENV_wazuh missing ENROLL_PASSWORD}" | sudo tee /opt/data/wazuh/authd.pass >/dev/null
sudo chmod 644 /opt/data/wazuh/authd.pass
```

`chmod 644` is because the manager container may not share the host uid; 644 on a file under `/opt/data` that only local users can reach is acceptable. Do **not** put this file in git.

Make it executable: `chmod +x wazuh/first-up.sh`.

**Step 2: Verify script parses**

```bash
bash -n wazuh/first-up.sh
```

Expected: no output, exit 0.

**Step 3: Commit**

```bash
git add wazuh/first-up.sh
git commit -m "Add idempotent Wazuh first-up for certs and sysctl."
```

---

### Task 5: Native agent install + enroll (bootstrap and deploy)

**Files:**
- Create: `wazuh/install-agent.sh`
- Modify: `bootstrap.sh` (after Docker / directories section)

**Step 1: Write `wazuh/install-agent.sh`**

```bash
#!/usr/bin/env bash
# Install and enroll wazuh-agent 4.14.7 against localhost. Idempotent.
set -euo pipefail
cd "$(dirname "$0")"
set -a && source .env && set +a

WAZUH_VER=4.14.7-1
if ! dpkg -s wazuh-agent >/dev/null 2>&1; then
  sudo install -d /usr/share/keyrings
  curl -fsSL https://packages.wazuh.com/key/GPG-KEY-WAZUH | sudo gpg --dearmor -o /usr/share/keyrings/wazuh.gpg
  echo 'deb [signed-by=/usr/share/keyrings/wazuh.gpg] https://packages.wazuh.com/4.x/apt/ stable main' \
    | sudo tee /etc/apt/sources.list.d/wazuh.list >/dev/null
  sudo apt-get update -q
  sudo WAZUH_MANAGER=127.0.0.1 \
       WAZUH_REGISTRATION_PASSWORD="${ENROLL_PASSWORD:?}" \
       WAZUH_AGENT_NAME="$(hostname -s)" \
       apt-get install -y "wazuh-agent=${WAZUH_VER}"
  sudo apt-mark hold wazuh-agent
fi

# Ensure manager address is localhost even if the package was installed earlier
sudo sed -i 's#<address>.*</address>#<address>127.0.0.1</address>#' /var/ossec/etc/ossec.conf
sudo systemctl enable --now wazuh-agent
sudo systemctl restart wazuh-agent
```

`chmod +x wazuh/install-agent.sh`.

**Step 2: Call sysctl + dirs from `bootstrap.sh`**

After the existing `install -d` block (~line 86), add:

```bash
# Wazuh indexer needs this; persist so it survives reboot
echo 'vm.max_map_count=262144' > /etc/sysctl.d/99-wazuh.conf
sysctl --system >/dev/null
install -d -o "$DEPLOY_USER" -g "$DEPLOY_USER" /opt/data/wazuh /opt/data/wazuh/certs
install -d -o 1000 -g 1000 /opt/data/wazuh/indexer
```

Do **not** install the agent in bootstrap (no `ENV_wazuh` yet). Agent install is deploy-time.

**Step 3: Verify**

```bash
bash -n wazuh/install-agent.sh
grep -n 'vm.max_map_count=262144' bootstrap.sh
```

Expected: script parses; bootstrap has the sysctl line.

**Step 4: Commit**

```bash
git add wazuh/install-agent.sh bootstrap.sh
git commit -m "Install the native Wazuh agent against localhost on deploy."
```

---

### Task 6: Wire `wazuh` into the deploy workflow

**Files:**
- Modify: `.github/workflows/deploy.yml`

**Step 1: Add stack to matrix**

Change:

```yaml
        stack: [traefik, monitoring, hermes, n8n, keycloak]
```

to:

```yaml
        stack: [traefik, monitoring, hermes, n8n, keycloak, wazuh]
```

**Step 2: After “Deploy ${{ matrix.stack }} stack”, add a wazuh-only hook**

Insert a new step **before** “Deploy ${{ matrix.stack }} stack” (certs/dirs must exist before compose up) and a second step after:

Before deploy:

```yaml
      - name: Wazuh first-up
        if: steps.diff.outputs.changed == 'true' && matrix.stack == 'wazuh'
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd /opt/infra/wazuh && bash first-up.sh"
```

The `.env` inject step already ran (it comes before deploy). Keep that order: Sync → Inject `.env` → Wazuh first-up → Deploy → Wazuh agent.

After deploy:

```yaml
      - name: Wazuh host agent
        if: steps.diff.outputs.changed == 'true' && matrix.stack == 'wazuh'
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd /opt/infra/wazuh && bash install-agent.sh"
```

**Step 3: Verify workflow YAML**

```bash
grep -n 'wazuh' .github/workflows/deploy.yml
```

Expected: matrix list includes `wazuh`; two steps reference `first-up.sh` and `install-agent.sh`.

**Step 4: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "Deploy the Wazuh stack and enroll the host agent."
```

---

### Task 7: Dashboard SAML snippets (files in git)

**Files:**
- Modify: `wazuh/config/wazuh_dashboard/opensearch_dashboards.yml`
- Modify: `wazuh/config/wazuh_dashboard/wazuh.yml`
- Create: `wazuh/config/wazuh_indexer/config.yml` (extracted + patched)
- Create: `wazuh/config/wazuh_indexer/roles_mapping.yml`

**Step 1: Dashboard YAML**

Append to `opensearch_dashboards.yml`:

```yaml
opensearch_security.auth.multiple_auth_enabled: true
opensearch_security.auth.type: ["basicauth","saml"]
server.xsrf.allowlist: ["/_opendistro/_security/saml/acs", "/_opendistro/_security/saml/logout", "/_opendistro/_security/saml/acs/idpinitiated"]
```

Keep `run_as: true` in `wazuh.yml` (already true upstream). Leave the API password as the demo value that matches `API_PASSWORD` in `.env.example`.

**Step 2: Extract default indexer security config**

On a machine with Docker (or during first VPS deploy):

```bash
docker run --rm --entrypoint cat wazuh/wazuh-indexer:4.14.7 \
  /usr/share/wazuh-indexer/config/opensearch-security/config.yml \
  > wazuh/config/wazuh_indexer/config.yml
docker run --rm --entrypoint cat wazuh/wazuh-indexer:4.14.7 \
  /usr/share/wazuh-indexer/config/opensearch-security/roles_mapping.yml \
  > wazuh/config/wazuh_indexer/roles_mapping.yml
```

If Docker is unavailable locally, SSH to the VPS after Task 6’s first deploy and copy those two files out of the running indexer container, then commit them.

**Step 3: Patch `config.yml`**

In `basic_internal_auth_domain`: `order: 0`, `challenge: false`.

Add under `authc:`:

```yaml
      saml_auth_domain:
        http_enabled: true
        transport_enabled: false
        order: 1
        http_authenticator:
          type: saml
          challenge: true
          config:
            idp:
              metadata_url: https://keycloak.${DOMAIN}/realms/XhampsHub/protocol/saml/descriptor
              entity_id: https://keycloak.${DOMAIN}/realms/XhampsHub
            sp:
              entity_id: wazuh-saml
            kibana_url: https://wazuh.${DOMAIN}
            roles_key: Roles
            exchange_key: ${SAML_EXCHANGE_KEY}
        authentication_backend:
          type: noop
```

`${DOMAIN}` and `${SAML_EXCHANGE_KEY}` will **not** expand inside a bind-mounted YAML. Either:

- Generate `config.yml` in `first-up.sh` from a template with `sed`/`envsubst` into `/opt/data/wazuh/config.yml` and mount **that**, or
- Put the real domain in the file (this VPS has one `DOMAIN` variable).

Use a template `wazuh/config/wazuh_indexer/config.yml.tmpl` and in `first-up.sh`:

```bash
SAML_EXCHANGE_KEY="${SAML_EXCHANGE_KEY:?}"
export DOMAIN SAML_EXCHANGE_KEY
envsubst < config/wazuh_indexer/config.yml.tmpl | sudo tee /opt/data/wazuh/indexer-security/config.yml >/dev/null
```

Add to `ENV_wazuh`: `SAML_EXCHANGE_KEY=<openssl rand -hex 32>`.

Mount `/opt/data/wazuh/indexer-security/config.yml` over the image’s `opensearch-security/config.yml`. Same for `roles_mapping.yml` after patching `all_access.backend_roles` to include `wazuh-admins`.

After indexer is up, apply securityadmin **once** if the image does not load the bind-mount on its own (first boot with `allow_default_init_securityindex: true` usually does). If SSO is missing, run:

```bash
docker compose exec wazuh.indexer bash -c '
  export JAVA_HOME=/usr/share/wazuh-indexer/jdk
  bash /usr/share/wazuh-indexer/plugins/opensearch-security/tools/securityadmin.sh \
    -cd /usr/share/wazuh-indexer/config/opensearch-security \
    -icl -nhnv \
    -cacert /usr/share/wazuh-indexer/config/certs/root-ca.pem \
    -cert /usr/share/wazuh-indexer/config/certs/admin.pem \
    -key /usr/share/wazuh-indexer/config/certs/admin-key.pem \
    -h wazuh.indexer
'
```

**Step 4: Mount the generated security files in compose**

Add to `wazuh.indexer` volumes:

```yaml
      - /opt/data/wazuh/indexer-security/config.yml:/usr/share/wazuh-indexer/config/opensearch-security/config.yml:ro
      - /opt/data/wazuh/indexer-security/roles_mapping.yml:/usr/share/wazuh-indexer/config/opensearch-security/roles_mapping.yml:ro
```

**Step 5: Commit**

```bash
git add wazuh/config traefik wazuh/docker-compose.yml wazuh/first-up.sh wazuh/.env.example
git commit -m "Enable Wazuh dashboard SAML against Keycloak."
```

---

### Task 8: Docs and Keycloak runbook (manual once)

**Files:**
- Modify: `README.md` (layout table: add `wazuh/`)
- Modify: `docs/runbook.md`

**Step 1: README layout row**

```
| `wazuh/` | Wazuh SIEM: indexer, manager, dashboard at `wazuh.<domain>`, native agent on the host |
```

**Step 2: Runbook section “Wazuh”**

Include:

1. GitHub secret `ENV_wazuh` (copy from `wazuh/.env.example`, set `ENROLL_PASSWORD` and `SAML_EXCHANGE_KEY` with `openssl rand -hex 16` / `openssl rand -hex 32`). `INDEXER_PASSWORD` / `DASHBOARD_PASSWORD` / `API_PASSWORD` must match the vendored hashes until rotated.
2. Re-run `bootstrap.sh` **or** just push — `first-up.sh` sets `vm.max_map_count` on deploy.
3. Keycloak (`XhampsHub` realm), SAML client:
   - Client ID: `wazuh-saml`
   - Valid redirect URIs: `https://wazuh.<domain>/*`
   - IDP-Initiated SSO URL name: `wazuh-dashboard`
   - Client signature required: Off
   - ACS POST: `https://wazuh.<domain>/_opendistro/_security/saml/acs/idpinitiated`
   - Logout redirect: `https://wazuh.<domain>`
   - Realm role `wazuh-admins` on a group; mapper on `role_list` with Role attribute name `Roles`, Single Role Attribute On
4. After first SSO login: Dashboard → Server management → Security → Roles mapping: `backend_roles` FIND `wazuh-admins` → role `administrator`.
5. Uptime Kuma HTTPS check for `wazuh.<domain>`.
6. Verify: `ss -lntp | grep -E '1514|1515'` shows `127.0.0.1` only; `https://wazuh.<domain>` has a public cert; Agents summary shows this host.

**Step 3: Commit**

```bash
git add README.md docs/runbook.md
git commit -m "Document Wazuh deploy, secrets, and Keycloak SAML."
```

---

### Task 9: Verification on the VPS

No unit tests. After push to `main` (or `workflow_dispatch` stack `wazuh`):

```bash
ssh deploy@VPS 'docker compose -f /opt/infra/wazuh/docker-compose.yml ps'
ssh deploy@VPS 'systemctl is-active wazuh-agent'
ssh deploy@VPS 'ss -lntp | grep -E "1514|1515|9200|55000|5601" || true'
curl -sI "https://wazuh.$DOMAIN" | head -n 20
```

Expected:

1. indexer, manager, dashboard `running` (healthy).
2. `wazuh-agent` is `active`.
3. 1514/1515 on `127.0.0.1` only; 9200/55000/5601 not published on `0.0.0.0`.
4. `curl -I` is 200/302; certificate is Let’s Encrypt (not the dashboard self-signed cert).
5. Break-glass `admin` / `INDEXER_PASSWORD` reaches the UI.
6. Keycloak user with `wazuh-admins` gets admin after the dashboard role mapping.
7. Agents → Summary shows this VPS connected.

If indexer OOM: confirm heap is `-Xms1g -Xmx1g` and free RAM; do not raise heap on an 8 GB box that already runs Grafana/Keycloak/n8n.

Rollback: `workflow_dispatch` previous successful run, or redeploy previous SHA. `docker compose down` does not uninstall `wazuh-agent`.

---

## Out of scope (do not do in this plan)

- Docker `wazuh-agent` container
- Wazuh 5.x
- Publishing 1514/1515 on `0.0.0.0`
- Automating the Keycloak client
- OpenID instead of SAML
- Rotating the demo indexer hashes in the same change set (follow-up)
